# Overview over Literature research

1) Loss_function paper: <br>
Paper about a suitable mixed loss function for image reconstruction/denoising -> uses convolution approach, based on mean and vairance of filter kernel, mixed with weihted L1 loss to provide good reconstructions, especially in high contrast contexts.

Originally proposed by NVIDA: SM-SSIM + L1 Loss